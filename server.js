// DEPENDENCIES 
const express = require('express');
const app = express();
// const path = require ('path');
const mongoose = require('mongoose');
const routes = require('./routes');
const PORT = process.env.PORT || 8080;

// DEFINE MIDDLEWARE AND STATIC ASSETS

// API ROUTES 

// MONGO DB CONNECT 

// START API SERVER 